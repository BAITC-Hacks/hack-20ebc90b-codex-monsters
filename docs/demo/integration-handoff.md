# Передача части C

## Публичное демо: фиксированное подключение

23.09.2026: добавлен `BUYER_LOCK_CONNECTION=true` для размещённого UI. Посетитель не может переключить реальный расчёт на mock или направить серверные HTTP-запросы на произвольный адрес. Режим закреплён за HTTP и серверным `BUYER_API_URL`, включая проверку изменённого состояния виджетов. Без флага локальный выбор mock/HTTP сохранён. API-контракты и дизайн не менялись; авторизация пользователей остаётся ограничением MVP.

Польза / критерии: воспроизводимый показ единой системы, результат и качество, презентация/демо. Проверка: `.venv/bin/python -m pytest -q tests/ui/test_hosted_config.py tests/ui/test_orders_app.py tests/ui/test_live_backend.py` — **27 passed, 3 subtests passed**; Ruff и `git diff --check` — PASS. В проверку входит работа UI с настоящим локальным API, утверждение/CSV и перезапуск backend. Проверка локальных тестов не подтверждает доступность внешнего URL; её выполняет A после размещения.

Для A: на публичном экземпляре установить флаг, передать внутренний адрес API и сохранить единственный backend worker с выделенным `EKT_DATA_DIR`. Для B изменений нет. Для C: после получения main перезапустить Streamlit; настройка описана в [runbook](runbook.md). Commit и публикацию сообщает интегратор после общей проверки.

## Актуальное дополнение: рабочее место закупщика

23.09.2026: реализован редизайн по запросу пользователя — светлая система с бирюзовым акцентом, маршрут из трёх шагов, товары рядом с итогом заказа, прямой выбор товара и помощь по запросу. API, расчёты и зависимости не менялись; синтетические подписи и утверждение конкретной версии сохранены. UI: **64 passed, 30 subtests passed**, Ruff — PASS. [Изменения, команды проверки, браузерные доказательства и ограничения](ui-redesign.md); [действующая дизайн-система](../../DESIGN.md). A/B/C: после получения опубликованной main перезапустить Streamlit из корня; для показа новой модели создать новый run, исторический `fixture-v1` остаётся историческим. Commit и публикацию подтверждает интегратор после push.

## Актуальное дополнение: дизайн-навыки и обратная связь

23.09.2026: оформление трёх разделов уточнено по `minimalist-ui`, `apple-design` и `emil-design-eng`: нейтральная палитра с обеими темами, явное выделение раздела, тонкие разделители, видимый фокус, подписи выполняющихся HTTP-операций. [Источники пяти установленных навыков, ограничения и проверки](design-skills.md).

После fast-forward на `d52d920`: **255 passed, 45 subtests passed**, Ruff и `git diff --check` проходят. В браузере проверены три раздела, клавиатура, обе темы, ширина 390 px, поиск и подготовка CSV через API.

A/B/C: получить актуальную `main` и перезапустить Streamlit. Навыки установлены локально и не нужны для запуска приложения. API и зависимости не менялись.

## Актуальное дополнение: упрощение UI

Получен актуальный main `2b7dce6` с модулем B. Пересобрана навигация: «Заказы», «Что, если…», «Данные». Служебные настройки и объяснения раскрываются по запросу, таблица очищена от технических ID, появился поиск. Утверждение сразу подготавливает CSV; повтор после сбоя экспорта не утверждает заказ повторно. Неактивные разделы не исполняются. API-контракты и зависимости не менялись.

Проверка: **235 passed, 45 subtests passed**, Ruff — PASS. Выполнен ручной браузерный проход на сохранённом synthetic run. Польза: ясность работы закупщика, качество сквозного результата и демонстрации; количественных выводов о бизнес-эффекте нет. [Описание и доказательства](ui-simplification.md).

Для A/B: забрать main и перезапустить UI; менять API не требуется. Для C/демонстратора: пользоваться обновлённым runbook и новыми названиями действий. Исторический demo run с fixture-v1 остаётся историческим, даже после подключения B; для демонстрации новой модели создать новый расчёт.

## Историческая передача первоначального MVP

UI `c0007e38253b102d8432bb02d4195aca44079292` проверен с последней базой A `2c72a42`; объединённый код `9bcbf7b`. Работа перенесена в main по новым AGENTS.md. Общие контракты, зависимости и файлы A/B не правились частью C.

Сданы: три вкладки, supplier proposals/ledger, редактирование line_id с reason/expected_version, свежая проверка version/hash перед approval/export, серверный CSV, реальные сценарные ответы и snapshot/job/run workflow. HTTP не переключается на mock; неоднозначные запросы не объявляются успешными.

Проверено: **123 теста** общего проекта, включая **3 настоящих HTTP/Streamlit integration tests**. Approval и идентичный CSV сохраняются после нового клиента, новой UI-сессии и перезапуска backend. [Отчёт](acceptance-report.md), [команды запуска](runbook.md).

UI согласован с фактическим payload A: `baseline_total_cost`, `baseline_base_qty/scenario_base_qty/delta_base_qty`, ключ SKU/supplier/warehouse вместо отсутствующего line_id. Единицы берутся из соответствующего proposal; неопределённые SS/ROP не подставляются. База сценария — исходный run/v1, ручные правки не объявляются базой what-if.

## Оставшиеся зависимости

1. B: подключить ingestion, forecast и demand-events. Server fallback `fixture-v1` помечен, LIVE project table пустая. PASS mock-классификации не подтверждает реальный detector.
2. A уже добавил seed/policy/as_of в run response. UI использует их, если они возвращены; для ранее сохранённых runs с null сохраняется явно помеченный fallback request seed. Snapshot дополнительно сверяется через proposals.
3. A: при необходимости добавить SS/ROP в scenario changed_lines. Сейчас показаны только возвращённые количества и стоимость.
4. Команда: проверить Python 3.11 и frozen-lock запуск на другом checkout. Тесты выполнены на Python 3.12.14; uv.lock содержит Streamlit 1.64.0 для отключения встроенного экспорта таблиц.

Mock reset очищает только имитацию текущей сессии. Для нового LIVE-демо можно остановить собственный backend и запустить его с новым выделенным EKT_DATA_DIR; UI не удаляет базы или файлы.

## Упрощение интерфейса по обратной связи, 23.09.2026

Актуальные вкладки: «План закупки», «Сравнение вариантов», «Данные». Основные параметры, объяснение количества и правка открыты; JSON выполнения заменён обычным статусом и датами, технические сведения выделены для поддержки. Декоративная полоса над итогом удалена. API и внутренние ключи навигации сохранены. После получения main перезапустить Streamlit; backend не требует перезапуска. Проверки и снимки — в [отчёте редизайна](ui-redesign.md#уточнение-после-обратной-связи-пользователя-23092026).

**Последующее исправление:** панели «Для поддержки» и все выводы JSON/кода удалены из пользовательского интерфейса целиком. Это отменяет предыдущее решение оставлять диагностику в раскрытиях. Полезные причины ошибок, ограничения и статусы сохранены обычным текстом. Mapping/policy версии берутся из прежних `session_state.mapping_version/policy_version`; настройка нового контракта не требуется. Выбор готового набора и восстановление задания сохранены как действия. После получения этого пакета также перезапустить только Streamlit.
